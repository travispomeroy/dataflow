/**
 * Dataflow Configs and their Draft → Deploy versioned lifecycle, including the
 * Execution Plan snapshot frozen per Deployment (ADR-0002) (M1).
 */
package dev.pomeroy.dataflow.controlplane.dataflow;
