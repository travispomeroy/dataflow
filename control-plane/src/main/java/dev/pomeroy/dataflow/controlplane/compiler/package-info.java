/**
 * Compilation: resolving a Dataflow Config against the Catalog into an Execution Plan.
 * Engine-specific artifact generation lives in the per-engine compiler modules (M1).
 */
package dev.pomeroy.dataflow.controlplane.compiler;
