/**
 * The Catalog: the curated registry of Sources, Destinations and Business Rules that
 * Dataflows are built from — the boundary where hidden complexity lives (M1).
 */
package dev.pomeroy.dataflow.controlplane.catalog;
